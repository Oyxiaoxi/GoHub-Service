# 应用基本配置
APP_NAME=GoHub-Service
APP_ENV=local
APP_KEY=zBqYyQrPNaIUsnRhsGtHLivjqiMjBVLS
APP_DEBUG=true
APP_URL=http://localhost:3000/
APP_PORT=3000

# 数据库配置
DB_CONNECTION=sqlite
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=GoHub-Service
DB_USERNAME=root
DB_PASSWORD=secret
DB_DEBUG=2

# Redis 配置
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_CACHE_DB=0
REDIS_MAIN_DB=1

# JWT 配置
JWT_EXPIRE_TIME=120
JWT_MAX_REFRESH_TIME=86400

# 邮件服务配置
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=GoHub-Service@example.com
MAIL_FROM_NAME=GoHub-Service

# 短信服务配置（以阿里云短信服务为例）
SMS_ALIYUN_ACCESS_ID=XXX
SMS_ALIYUN_ACCESS_SECRET=XXXXX
SMS_ALIYUN_SIGN_NAME=
SMS_ALIYUN_TEMPLATE_CODE=

# 验证码配置
VERIFY_CODE_LENGTH=6
VERIFY_CODE_EXPIRE=15

# 日志配置
LOG_TYPE=single
LOG_LEVEL=debug

# 文件存储配置（路径相关）
STORAGE_BASE_PATH=public/uploads
STORAGE_PUBLIC_PREFIX=/uploads
# 注意：文件大小限制、图片尺寸等业务常量已移至 config/app_constants.go

# CORS 跨域配置
# 开发环境示例：http://localhost:3000,http://localhost:3001,http://127.0.0.1:8080
# 生产环境示例：https://yourdomain.com,https://www.yourdomain.com
# 允许所有来源（不推荐生产环境）：*
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001,http://127.0.0.1:8080

# 限流配置
LIMITER_DEFAULT_MESSAGE=请求过于频繁，请稍后再试
LIMIT_GLOBAL_IP_RATE=200-H
LIMIT_AUTH_IP_RATE=1000-H
LIMIT_SIGNUP_EXIST_RATE=60-H
LIMIT_VERIFY_PHONE_RATE=20-H
LIMIT_VERIFY_EMAIL_RATE=20-H
LIMIT_VERIFY_CAPTCHA_RATE=50-H

# 内容安全配置（开关相关）
CONTENT_CHECK_ENABLED=true
SENSITIVE_WORD_FILTER_ENABLED=true
SENSITIVE_WORD_REPLACEMENT=***
XSS_PROTECTION_ENABLED=true
ALLOW_HTML_TAGS=false
IMAGE_CHECK_ENABLED=true
AUDIT_LOG_ENABLED=true
# 注意：内容长度限制、文件大小等业务常量已移至 config/app_constants.go
